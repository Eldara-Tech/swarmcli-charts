# The escape hatch: Renovate's own config.json as an external Docker config, at the
# default configFormat.
configName: renovate_config
dryRun: full
logLevel: debug
repositories:
  - Eldara-Tech/swarmcli
  - Eldara-Tech/swarmcli-charts
resources:
  limits:
    memory: 2G
