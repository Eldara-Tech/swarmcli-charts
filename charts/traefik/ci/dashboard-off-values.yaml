# Exercises the dashboard.enabled=false branch: no dashboard routers or
# basic-auth middleware, and --api=false. Traefik still proxies other services'
# routes, and the https-redirect middleware stays defined so routed charts can
# keep referencing it.
traefik:
  dashboard:
    enabled: false
  acme:
    email: admin@example.com
