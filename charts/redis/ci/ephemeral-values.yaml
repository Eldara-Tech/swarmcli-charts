# Ephemeral cache: no volume, no AOF, no node pin. Auth stays on. Exercises the
# no-volume branch (no top-level volumes block, no --dir/--appendonly, no placement).
persistence:
  enabled: false
placement:
  constraints: []
