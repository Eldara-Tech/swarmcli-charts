# Seed the initial root password from an external Swarm secret instead of letting GitLab
# generate one into /etc/gitlab/initial_root_password.
auth:
  rootPassword:
    enabled: true
    secretName: gitlab_root_password
