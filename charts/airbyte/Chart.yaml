name: airbyte
description: Airbyte data integration platform for Docker Swarm
version: 0.1.0
# renovate: image=airbyte/server
appVersion: "1.8.1"
# Oldest swarmcli whose chart engine renders this chart. Raise it when you use a
# newer feature, and only to a RELEASED version — scripts/floor-check.sh proves
# this by rendering with a real binary of it, and reports the floor as unverified
# if the version does not exist yet.
swarmcliVersion: ">= 1.13.0"
annotations:
  swarmcli-charts/allow: "docker-socket"
maintainers:
  - name: Eldara
    url: https://github.com/Eldara-Tech
keywords:
  - airbyte
home: https://github.com/Eldara-Tech/swarmcli-charts
sources:
  - https://github.com/Eldara-Tech/swarmcli-charts
