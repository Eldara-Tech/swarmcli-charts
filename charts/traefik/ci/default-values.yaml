# Default render case: a realistic production install — host-mode ports, HSTS on,
# secured dashboard with basic-auth. Mirrors the values an operator must supply.
traefik:
  acme:
    email: admin@example.com
  dashboard:
    host: traefik.example.com
    basicAuthUsers: "admin:$$apr1$$otIz5Cnl$$/y77Sc88rr1SfA6k0Ospw/"
