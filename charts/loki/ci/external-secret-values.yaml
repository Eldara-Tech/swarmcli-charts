# Same, but the configuration is a Swarm SECRET — the mode for a config carrying
# S3 credentials, which a Swarm config would expose to anyone with Docker access.
# It lands under /run/secrets, so -config.file follows it there. Render-only.
config:
  mode: external-secret
  externalName: loki-config
exposure:
  mode: published
