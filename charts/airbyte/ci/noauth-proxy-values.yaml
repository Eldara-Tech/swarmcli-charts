# No login in the chart, behind the operator's own authenticating proxy: the server and the
# connector builder join exposure.network with no Traefik labels.
auth:
  mode: none
exposure:
  mode: none
