# Ephemeral DB: no volume, no node pin. Auth stays on. Exercises the no-volume
# branch (no top-level volumes block) and that the node pin is dropped together
# with persistence — no placement block may render.
persistence:
  enabled: false
